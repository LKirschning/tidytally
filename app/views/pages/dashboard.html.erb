<h1>Tasks</h1>
<hr>
<div class="container">
<%sorted_tasks = @user.tasks.sort_by {|task| task.date} %>
<% sorted_tasks.each do |task|  %>
<div class="p-3 mt-2  text-center dashed-border">
<%= Date.parse("#{task.date}").strftime('%A') + ": " + task.room.name%>
<div>
<%= task.name %>
</div>
</div>
<% end %>
</div>
